# project-f0532417
A one-page Hello World web app, built to accept the team's pipeline end to end. It must use a database, a backend API and a frontend.- Content: the page shows a greeting read from the database. The stored greeting starts as "Hello, World!".- The visitor can change it: a text field and a Save button update the stored greeting, and the page then show
